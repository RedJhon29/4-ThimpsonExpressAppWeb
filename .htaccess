RewriteEngine On

# Si es un archivo o directorio real, servirlo directamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirigir todo a index.php (raíz)
RewriteRule ^(.*)$ index.php [QSA,L]
